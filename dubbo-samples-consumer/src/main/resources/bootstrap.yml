server:
  port: 9090
  servlet:
    context-path: /
spring:
  application:
    name: dubbo-samples-consumer
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://8.131.87.108:3306/students?useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=utf8
    username: stu
    password: 6666
  cloud:
    nacos:
      discovery:
        server-addr: 8.131.87.108:8848 #配置中心地址
      config:
        enabled: false
      username: nacos
      password: nacos

mybatis: #加上此配置解决返回数据部分字段为null的问题
  configuration:
    map-underscore-to-camel-case: true
pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql
  returnPageInfo: check
